# P02 evidence — T1 schema and safe projections

## Scope and disposition

- Phase: `P02`; task: `P02.T1 Define schema and safe API projections`.
- Result: `P02.T1` and `P02.T2` complete; `P02.C1`, `P02.C2` and `P02.C4` passed. P02 remains `in_progress`; P02.T3 and `P02.C3` remain pending. No P02 phase acceptance is claimed.
- Tested implementation baseline: `main` at `7f4b46396f2bd210908bb388f504b5278a489991` plus the T1 implementation diff; parent acceptance is recorded in the current P02.T1 commit. The final backend gates were rerun before the final documentation-only amend and no implementation files changed afterward. No source/reference database or production service was written.
- Backend: local pinned `trail v0.33.14-0-g3f965de7` / SQLite `3.53.2`, source `3f965de7ea516c43a54ca70a495e97f0c6d991ab`; binary SHA256 `0bfe77e850e1b3a30161555c4fb34f4eecf7c0f8193939fd02aa43aa29bf22eaf9`.
- Review: fresh independent read-only Luna review used `openai-codex/gpt-5.6-luna:max` and returned `READY FOR PARENT T1 REVIEW`, no findings. This is task evidence, not P02 phase acceptance.

## Acceptance-first checks

The initial P02 T1 checks were intentionally red against the P01-only schema; the private red logs are retained under `.artifacts/p02/` (`t1-schema-before*`, `t1-schema-direct-red*`). The implementation added three versioned migrations, safe read-only projections, isolated synthetic accounts/data and the backend selector/tests.

Parent reran the final gates with the pinned binary directory first in `PATH`; the runner now rebuilds the test-only WASM component before either backend selector so the pinned deferred probe cannot be stale:

```text
PATH="$PWD/.local/tools/trailbase-v0.33.14-darwin-arm64:$PATH" pnpm test:backend -- schema
exit 0; schema-two-depots 3/3, projection-rest-expand-sse 1/1, constraint-partial-races 1/1; skipped 0

PATH="$PWD/.local/tools/trailbase-v0.33.14-darwin-arm64:$PATH" pnpm test:backend -- capabilities
exit 0; pinned capabilities 8/8, cleanup ownership check passed; skipped 0

pnpm check
exit 0; svelte-check 0 errors, 0 warnings

pnpm exec eslint tests/backend/schema.test.ts scripts/test-backend.mjs tests/backend/stack.ts tests/backend/fixture/component.ts
exit 0

git diff --check
exit 0
pnpm plan:check && pnpm plan:status
exit 0; plan valid, P02 in_progress, P02.T1 complete, P02.C1 passed

Post-acceptance affected regression gates also passed: `pnpm lint` (ESLint clean), `pnpm test:unit` (2/2), `pnpm build` (static SvelteKit build), and `pnpm test:plan` (7/7). No retries or skips were used.
```

Fresh run logs include `.artifacts/p02/correction2-final-schema-20260912T174716Z.{stdout,stderr,exit}`, `.artifacts/p02/correction2-final-capabilities-20260912T174716Z.{stdout,stderr,exit}`, `.artifacts/p02/correction2-final-resources.txt`, and the corresponding correction-stage logs. These are private/ignored; raw backend logs remain mode `0600`.

## What the checks prove

- Two fresh owned depots have convergent normalized SQLite tables/views/FKs/indexes and generated insert/select/update schemas for all eight safe APIs; each depot survives restart without schema drift.
- `profiles` is native-linked (`is_uuid`, no UUIDv7 default); `questions` has positive integer IDs, unique `source_id`, and source NULL/empty metadata fidelity. Real SQLite checks enable foreign keys and reject malformed/absent native profile IDs, cross-game composite FKs, duplicate memberships/team names/round order, invalid state phases, immutable host changes, invalid rate buckets, invalid audit actors/actions, and partial transaction writes.
- The circular assignment/private/history contract is measured rather than inferred: the host SQLite probe and a pinned authenticated WASM transaction probe both reject an incomplete partner transaction at commit and commit a complete atomic partner set. The test also exercises both mismatch-insert directions, rollback/no residue, valid-partner preservation, and the append-only/immutable update guards. TrailBase's private `Unsupported DEFERRABLE` log warning is retained as diagnostic context; observed pinned transaction behavior is the acceptance oracle.
- REST/list/count/filter/order/read, generated schemas, expand denial, direct mutation denial, excluded-column probes, private/unconfigured API denial, unrelated ordinary-account isolation and authorized filtered SSE setup/cancellation are exercised across the eight public projections. No private base Record APIs are configured.
- The fixture uses synthetic verified ordinary accounts and an anonymous display identity outside the UI. It does not import questions, grant browser admin capability or claim auth/mail, avatar byte validation, handler CAS/idempotency, live revocation/event delivery, gameplay, or native behavior.

## Residual limitations and next action

- Full auth/local-mail/OAuth/profile/avatar behavior, device lifecycle, already-open SSE revocation/event-byte delivery and the complete ACCESS actor matrix remain P02.T2/T3 obligations.
- Production/test configuration separation, actual application WASM commands, question import and gameplay remain later phase work. `trail mcp` was not used as acceptance evidence.
- T2 red-check evidence and the corrected native resend boundary are recorded below; preserve P02.C1 evidence.

## P02.T2 acceptance-first red-check checkpoint — 2026-09-12

- Scope: `P02.T2` remains `in_progress`; no application auth, profile, avatar, return-to UI, or OAuth implementation was added. `P02.C2` remains pending and no P02 phase acceptance is claimed. Tested worktree baseline was `main` at `0d075aec294212ca00eb4b3457f153844f57e3bb`; the red-check/support diff is intentionally uncommitted at this checkpoint.
- Runtime: pinned `trail v0.33.14-0-g3f965de7` / SQLite `3.53.2`, source `3f965de7ea516c43a54ca70a495e97f0c6d991ab`; local Mailpit `v1.31.0` on macOS arm64. The auth stack verifies both versions, uses a fresh marked private depot, unique loopback TrailBase/SMTP/Mailpit HTTP ports and a private Mailpit database/log, and removes only owned children/depot. The pre-existing external Mailpit listeners on `127.0.0.1:8025` / `127.0.0.1:1025` were not used or modified.
- Initial red evidence was captured before auth/profile implementation in `.artifacts/p02/t2-red-correction-before.log`; the corrected final red run is `.artifacts/p02/t2-red-correction-final-auth-2.log` (exit 1). The earlier resend assertion was invalid for the pinned engine: `verify_email/trigger` resolves only the verified `email` column, while a newly registered pending account has only `unverified_email`, so the privacy-preserving HTTP 200 correctly sends no message. The check now uses an owned verified synthetic fixture account for real resend delivery/link extraction and retains the pending registration link independently.
- Final named failures are genuine remaining gaps: `reset-local-link-single-use` observed the pinned native reset token accepted again at HTTP 200; `profile-command-present` observed the absent `/api/trail-party/profile` command at HTTP 404; and `return-to-origin` observed no SPA/sessionStorage intent (`undefined` instead of `/join?code=ABC123`). The reset result is a measured native limitation, not a claimed application pass; a one-use boundary must be decided/implemented before C2 can pass.
- The same red run proved the native portions that are already available: pending registration returned no session, protected access/login before verification were denied, registration and corrected resend delivered real local mail, verification reuse was rejected, verified login/status/reset/identity-preserving refresh succeeded, and direct logout caused refresh-token rejection. Tokens, passwords, links and raw mail remain in ignored private logs only.
- Regression checks after the correction: `PATH="$PWD/.local/tools/trailbase-v0.33.14-darwin-arm64:$PATH" pnpm test:backend -- schema` exit 0 (3/3, skipped 0); the same pinned capabilities selector exit 0 (8/8, skipped 0); `pnpm check` exit 0 (0 diagnostics); targeted ESLint exit 0; `pnpm plan:check` and `pnpm plan:status` exit 0; `git diff --check` exit 0. Cleanup inspection found no owned process, listener, depot or staged file. Private command logs are `.artifacts/p02/t2-red-correction-{schema,capabilities,check,eslint-2}.log` and the final red log above.
- Review: fresh narrow independent read-only Luna review used actual `openai-codex/gpt-5.6-luna:max` and returned `READY` with no findings for the corrected resend, pending/reuse assertions, version/ownership cleanup and no implementation scope creep. Two earlier broader review attempts timed out in the subagent harness without file changes; they are infrastructure failures, not acceptance evidence.

### Next exact action

The owner has now approved a narrow documented re-scope for the pinned native reset replay; the next action is dispatching the Luna/max implementation worker for the smallest real local-mail auth/profile and validated return-to-origin flow. Preserve this red evidence and rerun the auth, schema, capabilities and static gates after each implementation change.

## P02.T2 reset replay decision and correction — 2026-09-12

- Owner decision: the interactive decision on 2026-09-12 selected **A — explicitly re-scope to the pinned native limitation**. This authorizes characterization only; it does not claim replay is single-use or secure. The original one-use red requirement and Astra design/security finding remain preserved as residual-risk history.
- The bounded exact Astra review was runtime-verified from its session initialization as `openai-codex / gpt-6-astra / medium`; it returned `OWNER DECISION`, found the unchanged one-use contract impossible under the current pinned/static/no-token-table constraints, rejected a frontend/app-gate bypass, and specified the acceptance boundary: prove first password change, old-password rejection, distinct replay-password login and unchanged identity while recording the upstream residual.
- A Luna/max test-only correction renamed `reset-local-link-single-use` to `reset-local-link-replay-characterized` and added the distinct sessionless replay password/login oracle. Before correction, `.artifacts/p02/auth-reset-before-20260912T203135Z.stdout.log` exited 1 with the old replay rejection and profile/return-to reds. After correction, `.artifacts/p02/auth-reset-after-20260912T203252Z.stdout.log` exited 1 with only the intended missing profile command (HTTP 404) and missing return-to/sessionStorage behavior (`undefined` vs `/join?code=ABC123`); the replay itself returned HTTP 200 / `Password reset`, changed the password, and retained the native identity. Targeted ESLint and `git diff --check` both exited 0. Runtime versions are recorded in `.artifacts/p02/auth-reset-runtime-20260912T203344Z.log`.
- At this historical checkpoint C2 remained pending until the real profile command and SPA return-to validator/consume behavior were implemented and the residual reset behavior was documented. Avatar validation, Google OAuth success and P02.T3 authorization remain out of scope for this pass.

## P02.T2 current implementation and review checkpoint — 2026-09-12

- The T2 implementation/support diff is committed on `main` in the current P02.T2 commit; no reset or production publication occurred. This section supersedes the historical “next action: implement” wording above without deleting the preserved red checkpoint.
- Implemented scope: native auth/profile UI in `src/routes/auth/+page.svelte`; native-token session handling in `src/lib/backend/session.ts`; validated, origin-scoped one-shot return intent in `src/lib/return-to-origin.ts`; authenticated `backend/functions/profile.ts`; auth-only WASM deployment in `tests/backend/stack.ts`; and `pnpm dev` application-WASM build/install through `scripts/dev.mjs --with-app-wasm`. The role query is presentation-only. No app password/token table, avatar subsystem, OAuth success, gameplay or P02.T3 authorization was added.
- The pinned engine cannot resend verification to a pending `unverified_email`: `verify_email/trigger` returns its privacy-preserving HTTP 200 but queries only verified `email`. The UI now states that limitation and exposes no false resend action. `auth.test.ts` adds `pending-resend-no-delivery`; the verified-fixture resend remains a separate real Mailpit check.
- Profile security coverage now includes anonymous and unverified denial, safe-field output, unknown/forged-field rejection, UUIDv7 timestamp binding, same-operation idempotent replay, conflicting replay rejection and one audit row after replay. `tests/unit/session.test.ts` exercises exact native token shape, malformed persistence, definitive 401 status invalidation, retryable 503 status and logout-clear-on-transport-failure using a real loopback HTTP server.
- Return intent now survives a queryless trip back to `/auth` while an existing validated, unexpired intent is pending; an explicit invalid/empty `returnTo` still clears it. Shell coverage checks this preservation and the registration-screen native resend limitation.
- Post-fix pinned backend/auth validation: `PATH="$PWD/.local/tools/trailbase-v0.33.14-darwin-arm64:$PATH" pnpm test:backend -- auth` passed `auth-local-mail` and `return-to-origin` with zero failures/skips after the authenticated-unverified, fresh timestamp-mismatch and session corrections; the auth stack used TrailBase `v0.33.14-0-g3f965de7`, SQLite `3.53.2`, Mailpit `v1.31.0`, fresh owned loopback ports/depot and no external Mailpit. Full output is `.artifacts/p02/p02-t2-auth-final-20260912T2138Z.{stdout,stderr,exit}`. `pnpm test:unit` then passed 3 files/6 tests, including the session storage/status/logout boundary suite.
- Final pinned regression selectors also passed: schema `3/3` and capabilities `8/8` with zero skips in `.artifacts/p02/p02-t2-{schema,capabilities}-final-20260912T2146Z.{stdout,stderr,exit}`. The complete static/plan/diff run passed check, lint, unit, build, scaffold `3/3`, shell `4/4`, plan regression `7/7`, plan check/status and `git diff --check`; its private output is `.artifacts/p02/p02-t2-static-final-20260912T2146Z.log` with exit `0`.
- Parent acceptance rerun on current `main` commit `8fd00f3`: pinned `pnpm test:backend -- auth` passed `auth-local-mail` and `return-to-origin` `2/2`, zero failures/skips in `.artifacts/p02/p02-t2-auth-accept-20260913T0711Z.{stdout,stderr,exit}`. Fresh `check`, `lint`, `test:unit` (`6`), `build`, scaffold (`3/3`), shell (`4/4`), plan (`7/7`), plan check and diff gates passed; output is `.artifacts/p02/p02-t2-acceptance-static-20260913T0712Z.{log,exit}`.
- Post-feedback local-dev usability correction: `pnpm dev` now starts a fresh owned Mailpit v1.31.0 on separate loopback HTTP/SMTP ports, injects only that SMTP sink into the private dev config, prints its inbox URL and captures a real registration message without TrailBase admin credentials. The pinned `pnpm test:bootstrap` run passed `25/25`, including `mail-enabled dev stack exposes an owned local inbox without admin credentials`; no external Trailhead Mailpit listener was used.
- The focused real browser probe passed native registration, owned Mailpit verification, verified login, profile POST HTTP 200 with exactly the safe fields, `/lobby` return-target consumption and sessionStorage leakage checks against the current UI. Its private result is `.artifacts/p02/p02-t2-auth-ui-real-green-20260912T2142Z.log`. This is not P04's full authenticated multi-user browser gate; `/lobby` remains a later feature route, so the probe records the expected partial-phase navigation limitation.
- Independent Astra/medium read-only review first returned **FINDINGS** for pending-resend implication, missing profile/session boundary tests, queryless return-to erasure and stale handoff/evidence wording. Those implementation/UI/doc issues were corrected. A second fresh review identified three remaining test-oracle gaps: the first unverified check was tokenless, the timestamp mismatch was freshness-confounded, and transport failure/storage preservation was under-asserted. The current tests now use native anonymous promotion to create a genuinely authenticated-unverified identity, a fresh-but-mismatched UUIDv7 timestamp, real socket failure, and explicit storage/token assertions. A third fresh review found only two documentation/evidence issues: the API overstated untested expiry/mismatch/delivery/rate-limit cases, and the cited green run predated the latest corrections. Those are now corrected with the post-fix `2138Z` backend/unit output and the `2142Z` browser probe. The final fresh Astra/medium read-only review returned **READY** with no blocking findings; it is review readiness, not P02 acceptance. Review sessions: `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/2026-09-11T19-00-31-630Z_01a091d7-c30e-74d1-bfbc-13028eea6adb/7e0f0e59-2e9c-4f85-9003-fc336e5cb069/run-0/session.jsonl`, `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/2026-09-11T19-00-31-630Z_01a091d7-c30e-74d1-bfbc-13028eea6adb/bce86d0c-8cad-4ea3-b4cf-efeba5ce7bd2/run-0/session.jsonl`, and `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/2026-09-11T19-00-31-630Z_01a091d7-c30e-74d1-bfbc-13028eea6adb/88b51d37-4391-4a67-af8e-e1422f8d941e/run-0/session.jsonl`, plus final `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/2026-09-11T19-00-31-630Z_01a091d7-c30e-74d1-bfbc-13028eea6adb/533e80bb-87f8-45b4-89f8-a9d14910346c/run-0/session.jsonl`.
- Residuals: pinned reset JWT replay remains an explicit upstream risk under owner option A; immediate old access-JWT revocation is not claimed; Google/avatar/native secure storage and P02.T3 remain out of scope; no authenticated multi-user P04 E2E, native display or Android TV proof is claimed. P02.C3 and P02.T3 remain pending; no P02 phase acceptance is claimed.
