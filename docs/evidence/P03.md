# P03 evidence — safe question import

## Scope and disposition

- Phase: `P03`, status `complete`; tasks `P03.T1`/`T2`/`T3` are complete and `P03.C1`–`C4` passed. The phase remains subject to the normal independent review checkpoint before later feature work.
- P03.T3 implements and tests the full private corpus gate in commit `4d9917a26626ada9a2142772e226eecedd1fb765`; exact committed gates are `.artifacts/p03/p03-t3-committed-final.log`. No public corpus publication or production import is claimed.
- P03.T2 implementation commit: `201d3c309cf35791f54339a6db460665bff02b20`; exact committed rerun is `.artifacts/p03/p03-t2-committed-final.log`. P03.T1's implementation baseline was `596b2bde22c68d9f21c903a6313bb4774a345810`.
- Reference checkout: `~/dev/trivia-party`, HEAD `442890dda579c6cb108d2f4851816e4388207627`, unchanged from the recorded baseline for the referenced import scripts. The reference database was inspected through a read-only SQLite URI only; it was not modified.
- Backend pin remains TrailBase `v0.33.14-0-g3f965de7`, SQLite `3.53.2`. The T1 synthetic tests use SQLite stdlib fixtures and do not substitute for a real backend gate.

## Acceptance-first checks

The first red run was against the P02-only repository: `pnpm test:import` exited 1 because `scripts/import_questions.py` did not exist; the private summary is `.artifacts/p03/p03-t1-red.log`. The missing-source negative case already passed, while the six importer behavior cases failed for the expected missing implementation.

The minimum implementation then added:

- `scripts/import_questions.py`, using only Python stdlib `sqlite3`, URI `mode=ro`, SQLite backup snapshots, parameterized batched inserts and one target transaction;
- explicit `--source`, `--depot` and `--dry-run` flags;
- marked-depot, symlink/hard-link, cooperating-lock and source/destination collision checks;
- exact mapped-field comparison for idempotent reruns and source-change conflict rollback;
- canonical newline-delimited JSON SHA-256 digests and an ignored local manifest; and
- `tests/import/test_import.py` with a synthetic PocketBase-shaped database containing Unicode, newlines, empty/NULL optional values and duplicate `external_id` values.

## Verification

All commands ran on 2026-09-13 against the current working tree, with the pinned TrailBase directory first in `PATH`:

```text
pnpm install --frozen-lockfile  exit 0
pnpm test:import              exit 0; 7 tests passed, 0 skipped
pnpm check                    exit 0; svelte-check 0 errors/warnings
pnpm lint                     exit 0
pnpm test:unit                exit 0; 6 tests passed
pnpm build                    exit 0; static build
pnpm test:scaffold            exit 0; 3 tests passed, 0 skipped
pnpm plan:check               exit 0; 13 phases, 39 tasks, 52 criteria
pnpm test:plan                exit 0; 7 tests passed, 0 skipped
python3 -m py_compile ...     exit 0

git diff --check              exit 0
```

The synthetic green run is `.artifacts/p03/p03-t1-import-final.log`; the exact committed-code rerun is `.artifacts/p03/p03-t1-committed-final.log`; individual static-gate logs are under `.artifacts/p03/`. The tests prove dry-run creates a private snapshot but inserts zero application rows; full import preserves every mapped field and duplicate external IDs; rerun inserts zero additional rows with stable digests; malformed required/numeric values fail before target writes; changed source content conflicts without mutating the prior row; and unowned/locked depots are refused.

## P03.T2 — interruption, resume and manifest checkpoint

The T2 red run is `.artifacts/p03/p03-t2-red.log`: the new interruption test failed because the importer did not yet expose a progress barrier, and the manifest lacked snapshot/distribution fields. The implementation adds optional bounded `--progress` diagnostics to stderr, raises on SIGINT inside the target transaction so SQLite rolls back, and emits source snapshot count, zero error count and category/difficulty distributions in the local manifest.

The synthetic suite now passes `9/9` with zero skips. It interrupts a 3,000-row import after a committed-to-transaction batch is reported, observes a nonzero interrupted exit and zero target rows, then starts a new process and imports all rows. It also checks manifest counts/digests/distributions, while retaining T1's missing-source, dry-run, all-field/Unicode, malformed-row, idempotent-rerun, source-conflict and destination-safety cases. The pre-commit regression output is `.artifacts/p03/p03-t2-final-precommit.log`; the exact committed rerun is `.artifacts/p03/p03-t2-committed-final.log`. The plan check was corrected after an intermediate missing-comma failure in `docs/STATUS.json` and then passed.

## P03.T3 — private full-corpus import and verification

The real run used a fresh marker-owned local depot under `.local/test-runs/` and the pinned TrailBase binary. The reference source was opened through the importer’s SQLite `mode=ro` path; its SHA-256 before/after comparison was unchanged. The destination was initialized by starting/stopping pinned TrailBase, then the import ran with TrailBase stopped. No source DB, snapshot, manifest, question text or runtime depot is tracked or staged.

Sanitized results:

```text
source snapshot rows       591,183
invalid/rejected rows      0
dry-run application rows   0
actual imported rows       591,183
actual target total        591,183
source/target digest       0744fbc809d72bec6c7b2640b1fb80efad06fb792eebc0759dd93d01f6aa303b
category buckets           56,338
difficulty                 easy 221,390; hard 166,860; medium 202,933
integrity_check            ok
foreign_key_check errors   0
idempotent rerun           imported 0; skipped 591,183
```

`pnpm verify:corpus -- --source <snapshot> --depot <owned-depot>` recomputed the all-field canonical digest/distributions, ran SQLite integrity/FK checks with local compatibility predicates for TrailBase-only schema functions, and ran the import a second time. A second pinned TrailBase start exposed a local-only `questions_import_check` view that omitted answer/metadata fields; `GET /api/records/v1/questions_import_check/1` returned HTTP 200 with only `id`, `source_id`, category/subcategory/difficulty/question/level/imported_at keys. The backend stopped with exit 0 and the owned depot lock was absent afterward. API details are `.artifacts/p03/p03-t3-api.log` and the sanitized result file; raw corpus artifacts remain ignored/private.

Committed implementation/gate logs are under `.artifacts/p03/`: `.artifacts/p03/p03-t3-committed-final.log` plus the dry-run/import/verify/API logs; the pre-commit run is retained separately. This passes P03.C1 (10/10 synthetic import tests, zero skips), C2 (real dry-run/import count and zero rejected rows), C3 (canonical digest/distributions/integrity/FK/idempotent rerun) and C4 (read-only source, no staged corpus, safe local API retrieval).

## Current boundary and next action

P03 is complete after its evidence gates; no public corpus publication or production import is claimed. P04 is the next ready phase. **Next exact action:** begin P04.T1 with an isolated real browser-test stack and deterministic synthetic fixtures; keep this private full-corpus depot/snapshot local and ignored. Commit only sanitized counts, digests, commands and evidence; never commit the source DB, snapshot or raw manifest.
