# P01 evidence — shared shell and testing-tooling checkpoint

Date: 2026-09-11 UTC. P01.T1 is complete after review/follow-up verification; P01.C1 passed. **P01 is not complete:** T2/T3 and C2/C3 remain pending; C4 has only native-shell/cleanup evidence so far. STATUS is canonical.

## Identity and environment

- Base checkpoint: `63b93b6` on `main`; final implementation tested as an uncommitted worktree. Code/tooling manifest `.artifacts/p01/final-worktree.sha256` has SHA256 `8539fb4d5696a2d902ca8eb5e07318825537c9053efc4dcb741c4218c87528ff` (excludes docs/AGENTS/root README). Exact post-commit verification is recorded below when available. Initial partial-worktree history remains in `.artifacts/p01/` and commit `63b93b6`.
- Reference `~/dev/trivia-party` HEAD remains `442890dda579c6cb108d2f4851816e4388207627`; no drift. Inspected landing, routing and ThemeContext source for F01 role entry and persistent light/dark/system themes. No reference production writes or question DB access.
- macOS 26.6.2 (25G83), arm64; Node 26.7.0; pnpm 11.22.0; rustc/cargo 1.91.1; Xcode selected at `/Applications/Xcode.app/Contents/Developer`.
- `trail --version` exit 0: `v0.33.14-0-g3f965de7 (2026-09-10)`, SQLite 3.53.2. No TrailBase process/depot started.
- Owner requested main/no worktrees, then explicitly approved existing installed browser tooling. Those durable preferences are in AGENTS/TESTING.

## Browser-tooling decision and proof

Standard: repository-pinned **Playwright Test 1.63.0**, local `channel: 'chrome'`, no personal profile, no auth-state reuse between actors. Actual launched browser reports **153.0.8010.37**. Seven contexts stayed open together, kept independent theme preferences, and a second tab in the first context inherited its preference. This proves browser-context isolation, **not authenticated users, backend/SSE isolation or gameplay**; those remain P04 onward.

The first attempt used bundled Chromium v1243 and could not launch because its executable was absent. `pnpm exec playwright install chromium` failed on the official CDN with default timeouts. No timeout/retry settings were changed. This was infrastructure failure, not behavioral red. Owner then approved the installed Chrome channel: no global tooling upgrade/download was needed.

Agent-browser 0.23.4 was rejected by its wrapper (requires >=0.35.0, recommends 0.37.0); its doctor command was unavailable. A Chrome DevTools MCP new-page call timed out. These are unverified optional exploratory tools, not the project's acceptance runner. Their failures were not concealed by a success claim.

## Acceptance-first history and fixes

| Check / failure | Actual result and response |
|---|---|
| Initial `node --test scripts/scaffold.test.mjs` | Exit 1: missing commands/config, expected scaffold red (2 failed) |
| Chrome-tooling regression before config/docs change | Exit 1: missing `channel: 'chrome'`; passed after explicit config + TESTING contract |
| `pnpm build && pnpm test:shell` using installed Chrome, before UI | Exit 1: actual browser launched; landing/display headings missing. Behavioral red captured in `.artifacts/p01/chrome-red/` and `chrome-red.log` |
| Seven-context test before UI | Exit 1: theme combobox absent. Preserved process log; no authentication claim |
| `pnpm test:unit` before generated utility | Exit 1: missing `src/lib/utils`; generated helper and pinned dependencies subsequently satisfy class-merging assertion |
| shadcn initial init | `$lib` alias absent because `src/lib` did not exist; created it, ran Kit sync, verified generated paths. Vega/neutral init + button/native-select generation then succeeded |
| First assembled quality gate | Typecheck passed; lint failed on unresolved root/generic-button links. Fixed centrally: typed internal button pathnames + Kit resolve(), and resolved root link. No lint rule disabled; UI navigation tests reran |
| Frozen install after removing generated minimumReleaseAge exception | Exit 1: newly published @lucide/svelte 1.45.0 violated pnpm policy. 1.44.0 is stable, compatible and older than the cutoff. Direct add still encountered the old lockfile precheck; exact manifest pins + `pnpm clean --lockfile && pnpm install` regenerated a compliant lock. Frozen install then passed. No release-age exception retained |

Generator supplies reviewed shadcn source, not reference source copies. Default button/select are 44px; links are internal resolved routes; upstream license/provenance is retained in `src/lib/components/ui/README.md`. The Tauri template was trimmed to no frontend native commands, empty permissions, explicit CSP, exact Tauri/tauri-build pins and common static output. The stock CLI-generated icon is only a shell placeholder, not a copied reference asset or new brand.

## Verification results

On the final pre-review application code (`proc_ed81`, stdout last written 2026-09-11T19:30:47Z), then again after review follow-up (`proc_6b84`, stdout last written 2026-09-11T19:35:27Z):

```text
pnpm install --frozen-lockfile                            exit 0, normal supply-chain policies
pnpm check                                               exit 0, no Svelte errors/warnings
pnpm lint                                                exit 0
pnpm test:unit                                           exit 0, 1 passed
pnpm build                                               exit 0, adapter-static -> build/index.html
pnpm test:scaffold                                       exit 0, 3 passed, zero skips
pnpm test:shell                                          exit 0, 3 passed, zero retries/skips
cargo fmt --manifest-path src-tauri/Cargo.toml --check     exit 0
cargo build --locked --manifest-path src-tauri/Cargo.toml exit 0
pnpm plan:check                                          exit 0
pnpm test:plan                                           exit 0, 7 passed, zero skips
```

The second full run includes narrowed package engine metadata and the additional direct `/auth?role=host` deep-link/reload assertions. All commands again exited 0. Evidence captured at 2026-09-11T19:36:28Z; no code changes after that verified set.

**P01.C1 / F01, F28:** the browser suite covers landing/host-player entry destinations, keyboard activation, 375px layout, >=44px host action, persisted light/dark/system preferences including system changes, seven-context preference isolation, direct `/auth?role=host` and `/display` load/reload and no Tauri globals/page errors. Shell explicitly says authentication/games/pairing are unavailable. `tests/shell/static.config.ts` serves only `build/` through Vite's generic static preview (not Kit's SSR preview middleware). No server route, mocked backend, fake authentication or gameplay shortcut exists.

**P01.C4 partial / F24 foundation:** `pnpm tauri dev --no-watch` built and launched the real native WKWebView displaying the same `/display` heading/content/theme controls. Process manager `proc_9669`; application PID 29688; Vite port 5173; own native window 99212. CoreGraphics identified that exact owned window; `screencapture -x -l 99212 .artifacts/p01/native-display.png` captured it, and the image was visually inspected. SHA256 `4c0eb7ea98818b5ac88997280c78981f38be06a71a3a49fb973e5eec0cfcc379`. Only the app's synthetic window was captured, not the desktop. Process stop cleaned the app and dev server; PID absence and port 5173 clear verified. This is native dev-shell proof, not packaged/native gameplay, Android TV, multi-monitor, signing or update proof. T3's safe backend launcher and behavioral decisions still pending, so C4 is not passed.

## Committed verification and publication

Scaffold commit **`f1656b2cfec75ded660a2019174d072fc63b34cb`** was verified by `proc_cd55` (stdout last written **2026-09-11T19:39:15Z**). The run printed that exact HEAD, then repeated every command in the verification table above and `git diff --exit-code`: all exit 0; unit 1, scaffold 3, browser 3, plan regression 7; zero retries/skips. Logs: `.artifacts/p01/committed-gates.{stdout,stderr}.log`. Playwright again reports Chrome 153.0.8010.37; port 4173 was clear after teardown. Working tree was clean before this documentation-only evidence update.

Owner-authorized `git push origin main` succeeded; `git ls-remote origin refs/heads/main` returned the full scaffold SHA above. Staged paths were reviewed: code/config/docs/locks plus the stock generated icon only, no runtime DB/corpus/secrets/screenshots/logs or release artifacts. This publication is source Git only, not deployment/native release publication. The following checkpoint changes evidence/handoff only; application code remains the verified scaffold commit.

## Artifacts, review and limitations

Raw logs/traces/screenshots stay ignored under `.artifacts/p01/` and `.artifacts/shell/`. Process-manager logs are copied there for restart where available. Browser display screenshot was also inspected; all visible text is synthetic preview content. No DBs, secrets, personal browser profiles or original corpus were used/staged.

Independent fresh-context read-only reviewer `c9541d92-9d97-484a-933c-9f630f0b7341` found no blocking implementation defect and returned “OK with notes.” Output copied to `.artifacts/p01/review.md` from the tool's bound output artifact. The specific coverage note was valid: `/display` was the only directly loaded deep route, while `/auth` was only tested via navigation. Added direct `/auth?role=host` load/reload to the same error-checked test, and reran all gates successfully. Remaining note retained: context tests are preferences-only and not broad authenticated/native-platform acceptance. No unresolved T1 scope/security defect found.

Next task: **P01.T2**, failing real pinned-backend capability probes in an owned throwaway depot; do not prebuild generic client/realtime abstractions without those contracts. Google credentials, release signing, corpus rights and real native-device acceptance remain future gates, not blockers to T2.
