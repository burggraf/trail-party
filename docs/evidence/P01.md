# P01 evidence — shared shell and backend capability probes

Date: 2026-09-11 UTC. P01.T1/C1 are complete. T2 is complete after passing probes and independent review; C2/C3 passed. T3 is blocked; C4 remains pending. **P01 is not complete.** STATUS is canonical. The original T1 evidence below is historical; the T2 checkpoint follows it.

## Identity and environment

- Base checkpoint: `63b93b6` on `main`; final implementation tested as an uncommitted worktree. Code/tooling manifest `.artifacts/p01/final-worktree.sha256` has SHA256 `8539fb4d5696a2d902ca8eb5e07318825537c9053efc4dcb741c4218c87528ff` (excludes docs/AGENTS/root README). Exact post-commit verification is recorded below when available. Initial partial-worktree history remains in `.artifacts/p01/` and commit `63b93b6`.
- Reference `~/dev/trivia-party` HEAD remains `442890dda579c6cb108d2f4851816e4388207627`; no drift. Inspected landing, routing and ThemeContext source for F01 role entry and persistent light/dark/system themes. No reference production writes or question DB access.
- macOS 26.6.2 (25G83), arm64; Node 26.7.0; pnpm 11.22.0; rustc/cargo 1.91.1; Xcode selected at `/Applications/Xcode.app/Contents/Developer`.
- `trail --version` exit 0: `v0.33.14-0-g3f965de7 (2026-09-10)`, SQLite 3.53.2. No TrailBase process/depot started.
- Owner requested main/no worktrees, then explicitly approved existing installed browser tooling. Those durable preferences are in AGENTS/TESTING.

## Browser-tooling decision and proof

Standard: repository-pinned **Playwright Test 1.63.0**, local `channel: 'chrome'`, no personal profile, no auth-state reuse between actors. Actual launched browser reports **153.0.8010.37**. Seven contexts stayed open together, kept independent theme preferences, and a second tab in the first context inherited its preference. This proves browser-context isolation, **not authenticated users, backend/SSE isolation or gameplay**; those remain P04 onward.

The first attempt used bundled Chromium v1243 and could not launch because its executable was absent. `pnpm exec playwright install chromium` failed on the official CDN with default timeouts. No timeout/retry settings were changed. This was infrastructure failure, not behavioral red. Owner then approved the installed Chrome channel: no global tooling upgrade/download was needed.

Agent-browser 0.23.4 was rejected by its wrapper (requires >=0.35.0, recommends 0.37.0); its doctor command was unavailable. A Chrome DevTools MCP new-page call timed out. These are unverified optional exploratory tools, not the project's acceptance runner. Their failures were not concealed by a success claim.

## Acceptance-first history and fixes

| Check / failure | Actual result and response |
|---|---|
| Initial `node --test scripts/scaffold.test.mjs` | Exit 1: missing commands/config, expected scaffold red (2 failed) |
| Chrome-tooling regression before config/docs change | Exit 1: missing `channel: 'chrome'`; passed after explicit config + TESTING contract |
| `pnpm build && pnpm test:shell` using installed Chrome, before UI | Exit 1: actual browser launched; landing/display headings missing. Behavioral red captured in `.artifacts/p01/chrome-red/` and `chrome-red.log` |
| Seven-context test before UI | Exit 1: theme combobox absent. Preserved process log; no authentication claim |
| `pnpm test:unit` before generated utility | Exit 1: missing `src/lib/utils`; generated helper and pinned dependencies subsequently satisfy class-merging assertion |
| shadcn initial init | `$lib` alias absent because `src/lib` did not exist; created it, ran Kit sync, verified generated paths. Vega/neutral init + button/native-select generation then succeeded |
| First assembled quality gate | Typecheck passed; lint failed on unresolved root/generic-button links. Fixed centrally: typed internal button pathnames + Kit resolve(), and resolved root link. No lint rule disabled; UI navigation tests reran |
| Frozen install after removing generated minimumReleaseAge exception | Exit 1: newly published @lucide/svelte 1.45.0 violated pnpm policy. 1.44.0 is stable, compatible and older than the cutoff. Direct add still encountered the old lockfile precheck; exact manifest pins + `pnpm clean --lockfile && pnpm install` regenerated a compliant lock. Frozen install then passed. No release-age exception retained |

Generator supplies reviewed shadcn source, not reference source copies. Default button/select are 44px; links are internal resolved routes; upstream license/provenance is retained in `src/lib/components/ui/README.md`. The Tauri template was trimmed to no frontend native commands, empty permissions, explicit CSP, exact Tauri/tauri-build pins and common static output. The stock CLI-generated icon is only a shell placeholder, not a copied reference asset or new brand.

## Verification results

On the final pre-review application code (`proc_ed81`, stdout last written 2026-09-11T19:30:47Z), then again after review follow-up (`proc_6b84`, stdout last written 2026-09-11T19:35:27Z):

```text
pnpm install --frozen-lockfile                            exit 0, normal supply-chain policies
pnpm check                                               exit 0, no Svelte errors/warnings
pnpm lint                                                exit 0
pnpm test:unit                                           exit 0, 1 passed
pnpm build                                               exit 0, adapter-static -> build/index.html
pnpm test:scaffold                                       exit 0, 3 passed, zero skips
pnpm test:shell                                          exit 0, 3 passed, zero retries/skips
cargo fmt --manifest-path src-tauri/Cargo.toml --check     exit 0
cargo build --locked --manifest-path src-tauri/Cargo.toml exit 0
pnpm plan:check                                          exit 0
pnpm test:plan                                           exit 0, 7 passed, zero skips
```

The second full run includes narrowed package engine metadata and the additional direct `/auth?role=host` deep-link/reload assertions. All commands again exited 0. Evidence captured at 2026-09-11T19:36:28Z; no code changes after that verified set.

**P01.C1 / F01, F28:** the browser suite covers landing/host-player entry destinations, keyboard activation, 375px layout, >=44px host action, persisted light/dark/system preferences including system changes, seven-context preference isolation, direct `/auth?role=host` and `/display` load/reload and no Tauri globals/page errors. Shell explicitly says authentication/games/pairing are unavailable. `tests/shell/static.config.ts` serves only `build/` through Vite's generic static preview (not Kit's SSR preview middleware). No server route, mocked backend, fake authentication or gameplay shortcut exists.

**P01.C4 partial / F24 foundation:** `pnpm tauri dev --no-watch` built and launched the real native WKWebView displaying the same `/display` heading/content/theme controls. Process manager `proc_9669`; application PID 29688; Vite port 5173; own native window 99212. CoreGraphics identified that exact owned window; `screencapture -x -l 99212 .artifacts/p01/native-display.png` captured it, and the image was visually inspected. SHA256 `4c0eb7ea98818b5ac88997280c78981f38be06a71a3a49fb973e5eec0cfcc379`. Only the app's synthetic window was captured, not the desktop. Process stop cleaned the app and dev server; PID absence and port 5173 clear verified. This is native dev-shell proof, not packaged/native gameplay, Android TV, multi-monitor, signing or update proof. T3's safe backend launcher and behavioral decisions still pending, so C4 is not passed.

## Committed verification and publication

Scaffold commit **`f1656b2cfec75ded660a2019174d072fc63b34cb`** was verified by `proc_cd55` (stdout last written **2026-09-11T19:39:15Z**). The run printed that exact HEAD, then repeated every command in the verification table above and `git diff --exit-code`: all exit 0; unit 1, scaffold 3, browser 3, plan regression 7; zero retries/skips. Logs: `.artifacts/p01/committed-gates.{stdout,stderr}.log`. Playwright again reports Chrome 153.0.8010.37; port 4173 was clear after teardown. Working tree was clean before this documentation-only evidence update.

Owner-authorized `git push origin main` succeeded; `git ls-remote origin refs/heads/main` returned the full scaffold SHA above. Staged paths were reviewed: code/config/docs/locks plus the stock generated icon only, no runtime DB/corpus/secrets/screenshots/logs or release artifacts. This publication is source Git only, not deployment/native release publication. The following checkpoint changes evidence/handoff only; application code remains the verified scaffold commit.

## Artifacts, review and limitations

Raw logs/traces/screenshots stay ignored under `.artifacts/p01/` and `.artifacts/shell/`. Process-manager logs are copied there for restart where available. Browser display screenshot was also inspected; all visible text is synthetic preview content. No DBs, secrets, personal browser profiles or original corpus were used/staged.

Independent fresh-context read-only reviewer `c9541d92-9d97-484a-933c-9f630f0b7341` found no blocking implementation defect and returned “OK with notes.” Output copied to `.artifacts/p01/review.md` from the tool's bound output artifact. The specific coverage note was valid: `/display` was the only directly loaded deep route, while `/auth` was only tested via navigation. Added direct `/auth?role=host` load/reload to the same error-checked test, and reran all gates successfully. Remaining note retained: context tests are preferences-only and not broad authenticated/native-platform acceptance. No unresolved T1 scope/security defect found.

At the T1 checkpoint, the next task was P01.T2. Google credentials, release signing, corpus rights and real native-device acceptance remain future gates.

## P01.T2 — real backend contracts (2026-09-11)

### Identity, scope and provenance

Implementation worktree based on **`44a52e7a1721afe636006711539f1fd1a7695d76`**, branch `main`; no worktree or unrelated edits. The reference HEAD is still `442890dda579c6cb108d2f4851816e4388207627`. No source database read/import or production writes. Acceptance probes map to **F02/F21/F29 foundations**, not completed auth/gameplay parity. T2 was marked in_progress before implementation.

Same macOS/Node/pnpm/Rust/Chrome environment as T1. `trail --version` reports v0.33.14-0-g3f965de7 / SQLite 3.53.2. Installed executable `/Users/markb/.local/bin/trail` SHA256: `0bfe77e850e1b3a30161555c4fb34f4eecf7c0f81939fd02aa43aa29bf22eaf9`. This is an observed local executable digest, **not** a fabricated official release-archive digest; CI artifact download/digest verification remains T3.

Exact packages: `trailbase@0.14.1`, `trailbase-wasm@0.6.0`, `@bytecodealliance/jco@1.32.1`. Source/API files and CLI help were captured under ignored `.artifacts/p01-t2/upstream/` from full commit `3f965de7ea516c43a54ca70a495e97f0c6d991ab`, not current online examples. Vite 8.3.0 bundles `tests/backend/fixture/component.ts`; JCO uses the packaged WIT. JCO explicitly reports a compatibility fallback to **componentize-js 0.19.3** (locked dependency). Its resulting WASM passed real authenticated calls and atomicity assertions on the required backend; no global tool or backend upgrade occurred.

Pre-review tested component: `.artifacts/p01-t2/pre-review.probe.wasm`, SHA256 **`ecf6f5ed8e7649ba0ff2d4e6e8e9983c629b1c5c63b637b811bfa6ed12bb7196`**. Component bytes differ between builds; no bit-for-bit reproducibility claim. Record each actually tested artifact rather than reusing a prior digest.

### Red checks and corrections

| Check | Observed failure / response |
|---|---|
| `node --test tests/backend/capabilities.test.ts` before fixture implementation | Exit 1, `P01 probe configuration missing`; `.artifacts/p01-t2/initial-red.log` |
| First real initialization (`proc_1b05`) | Exit 1: `trail user add` references removed `verified` column. Migrations had run successfully. Source confirmed stale CLI SQL versus `U1785764695__unverified_email.sql`; no backend upgrade or direct auth-table workaround |
| Admin-API bootstrap (`proc_f2eb`) | Genuine backend/ordinary logins worked. CRUD rejected object payloads because `is_json` did not supply JSON API metadata; changed fixture constraint to `jsonschema_matches`. Denial checker now uses SDK `FetchError`, which implements but does not extend Error |
| JSON-metadata run (`proc_d250`) | CRUD/auth/ACL/ordinary SSE passed. WASM validation rejected the SDK's padded UUID; pinned Base64 implementation confirmed required `==`, and validation/regression now assert UUIDv7 + padding |
| Real byte-fragmentation probe (`proc_8410`) | WASM/CAS/rollback also passed; SSE byte-fragmentation failed within the original 2s bound. No event arrived because SDK discarded partial frames; no timeout/retry increase |
| `pnpm test:unit` sequence regression | Exit 1, `onLoss` count 0 instead of 1 across chunks; `.artifacts/p01-t2/sequence-red.log` |
| Patched SDK (`proc_f01a`) | Frozen install/type/lint/unit/backend all exit 0. The SDK now retains UTF-8/partial frames and sequence state; first green logs retained |

The tiny SDK patch is bound by `pnpm-workspace.yaml` and `pnpm-lock.yaml`. It modifies the SDK's existing subscription implementation, not a new client, event bus or PocketBase layer. Provenance/limitations and upstream Apache notice are retained in `patches/`; no project/reference/corpus license is assigned.

### Actual assertions and lifecycle

**C2:** `pnpm test:backend -- capabilities` starts real TrailBase, applies fresh migrations and exports insert/select/update schemas via the CLI. It proves empty fresh state, restart, UUIDv7/padded Base64/JSON/Unix-second serialization, create-ID/update-void/delete-void contracts, invalid JSON rejection, actual ordinary-account login/refresh/logout and rejected logged-out refresh-token reuse. Anonymous refresh retains identity; this is not verification/mail/OAuth acceptance.

SQL ACL checks reject unauthenticated access/subscription, foreign read/update/delete/subscription and forged owner/version writes. Genuine SSE tests prove filter and opponent exclusion, insert/update/delete delivery, cancellation, resubscription and Unicode content under **single-byte splitting of actual response bytes**. No API/event content is invented in these integration tests. The separate unit sequence test uses deterministic parser input and is not counted as real-backend acceptance.

**C3:** `HttpRequest.user()` supplies actual authenticated identity. One transaction performs an expected-version CAS plus an audit insert. Concurrent commands produce exactly one JSON success/version increment and one 409; unauthorized/invalid input is rejected. A deliberate second-write CHECK failure returns 409 and leaves both the first-write version and audit count unchanged. This synthetic-only endpoint/table/sentinel is not application/gameplay code and must not become a production bypass.

`tests/backend/stack.ts` accepts no external target. It allocates a fresh loopback port/marked depot, checks health plus a per-run schema API before auth mutations, and owns its child. Supported local admin promotion plus `/api/_admin/user` provisions verified **non-admin** baseline actors outside normal UI; no forged token, copied user, parsed password or direct auth-table write. Logs may contain auto-generated bootstrap credentials and remain private/ignored. Teardown stops its child and verifies marker/root before deletion; ownership checks reject unmarked/wrong-owner paths and the repository/test root. No unknown listener is killed.

### Full pre-review verification

`proc_9d5d` ran against this implementation worktree; logs copied at 2026-09-11T20:19:48Z to `.artifacts/p01-t2/pre-review.{stdout,stderr}.log`:

```text
pnpm install --frozen-lockfile                            exit 0, patch applied, no policy exemptions
pnpm check && pnpm lint                                  exit 0, no errors/warnings
pnpm test:unit                                           exit 0, 2 passed
pnpm build                                               exit 0, static output
pnpm test:backend -- capabilities                        exit 0, 8 passed, 0 skipped/cancelled
pnpm test:scaffold                                       exit 0, 3 passed
pnpm test:shell                                          exit 0, 3 passed, Chrome 153.0.8010.37
cargo fmt --manifest-path src-tauri/Cargo.toml --check     exit 0
cargo build --locked --manifest-path src-tauri/Cargo.toml exit 0
pnpm plan:check                                          exit 0
pnpm test:plan                                           exit 0, 7 passed
```

Zero acceptance retries/unexpected skips. Per-run backend port 57762 and shell port 4173 were clear after teardown; `.local/test-runs/` had no remaining depots. This is not the ten-consecutive full-game stability gate. Browser/native shell scope remains unchanged; no new native-window or Android TV acceptance claimed.

Fresh read-only reviewer **`adddf6f6-28b1-4345-a3fc-1e8e18f3ecfa`** checked the implementation/plan/evidence and returned **OK — ready, no issues found**. It did not rerun tests or inspect private raw backend logs; the supervisor's actual full run above supplies execution evidence. Bound output was copied to `.artifacts/p01-t2/review.md`. T2/C2/C3 accepted against the reviewed/tested implementation worktree; exact committed-code verification follows in the next evidence checkpoint, not retroactively attributed to the prior SHA.

### Exact committed-code verification

Commit **`516755d1b4a72c92a847e33ac7d41a0702309921`** was verified by **`proc_e63a`**, completed **2026-09-11T20:27:04Z**. The process printed the exact HEAD, repeated every command in the full verification table plus `pnpm plan:status`, and finished with `git diff --exit-code`: all exit 0. Unit 2, backend 8, scaffold 3, browser 3, plan regression 7; zero retries/skips. Chrome remained 153.0.8010.37. Logs: `.artifacts/p01-t2/committed.{stdout,stderr}.log`.

Actually tested component preserved as `.artifacts/p01-t2/committed.probe.wasm`, SHA256 **`b5fffbe8777380ba29e9dd5dd4fdca331b751ba7b844c67da0d7d3500f385a81`**. Backend port 58214 and shell port 4173 were clear after teardown; no test depots remained. Working tree was clean before this evidence-only update. Staged implementation paths and credential patterns were checked: no runtime DBs, corpus, credentials, raw logs, screenshots or WASM artifacts were committed. `.gitattributes` handles only the mandatory space-before-tab context prefix in generated unified patch files; application whitespace checks and functional assertions are unchanged.

This follow-up changes evidence/handoff only; implementation remains the exact tested commit above. Remaining work: P01.T3 product decisions/dev launcher/CI binary provenance, then actual P02 schema/auth/authorization. Membership/expiry revalidation, browser-auth persistence, automatic SSE recovery and gameplay remain explicitly unimplemented.

## Historical P01.T3 blocked recovery / partial work (2026-09-11)

**Historical rejected partial launcher; superseded by controlled repair below.** Both earlier delegated runs stopped before independent reviews. Main remains `a8364e0063fed863b1a4871964b7d3f8a0c61f82`; source baseline remains `442890dda579c6cb108d2f4851816e4388207627`. No T3 staging, commit, push, deployment or release occurred.

### Infrastructure failures and preserved evidence

1. Workflow `d49f4bd1-0e47-4be1-baea-8f9f429a88b7`: scout `49a34597-5243-465c-9f11-a447a7f22de1` requested Luna/medium but reported Sol, failing model verification. A separate workflow formatter bug emitted optional `undefined` metadata; it was corrected by omitting undefined values and passed a Node serialization regression and static workflow validation before recovery.
2. Recovery `ab6da36f-e839-4781-900d-6b4347843c7e`: implementer `4f6c6f2a-e3b5-48cb-886e-d03a378198a7` requested Astra/high but reported Luna. Its native session shows an actual startup change: Astra/high at `21:08:12.818Z`, Luna at `21:08:12.950Z`, then medium→low thinking before the first assistant response. All 26 assistant messages used Luna. This is not evidence supporting a response alias. The responsible module was subsequently confirmed below; owner decision is still required before configuration changes or another native run.

Ignored local preservation: `.artifacts/p01-t3/before-recovery.patch`, `failed-workflow.status.json`, `recovery-failed.patch`, `recovery-untracked.txt`, `recovery-untracked.tgz`, `recovery-failed.status.json`, and `model-routing.json`. No external/foreground agent fallback or unverified alias was used. Process list found no managed running processes; `.local/test-runs/` was empty after failure. No application launcher was started by the parent during this inspection.

### Parent inspection rejects the partial implementation

- `scripts/dev.mjs` has no exact backend version check and probes only `/api/healthcheck`, not schema/instance readiness. It announces frontend readiness before proving `/display` responds.
- Its ownership check can mark an existing unmarked directory as owned; an absent external depot can be created after `ENOENT` without a containment check. It does not enforce the required symlink/foreign-target boundary.
- TrailBase inherits terminal output instead of using a private log. Cleanup sends direct-child SIGTERM then exits after 100ms without waiting/escalating or proving descendant cleanup; successful backend exit is not handled as stack failure. These violate the approved safety contract.
- `scripts/bootstrap.test.mjs` contains only a manifest-shape check and an external-symlink refusal. It does not prove real successful readiness, persistent restart, unmarked/foreign-instance/version/occupied-port refusal, child failure or signal cleanup.
- The release manifest contains only macOS; CI separately duplicates a Linux pin and prints `--version` without asserting the exact backend/SQLite/source or running the implemented app gates. No reusable verified repo-local download/execution command or committed six-decision documentation was completed.

These are current-scope defects, not deferred native/CI polish. Preserve the partial files for a corrected, acceptance-first writer after routing is resolved; do not accept the worker's original safety/completeness prose.

### Actual checks versus claims

Runtime acceptance metadata independently executed `pnpm test:bootstrap` with exit 0, **2 tests**, zero skips, approximately 592ms total. This only verifies the two narrow assertions above, not C4. The child's claims of `pnpm check`, `pnpm lint`, `pnpm plan:check` and independent download hashes have not been parent-accepted as T3 execution evidence. The full requested regression suite, native startup/cleanup and Linux execution have not been accepted. Parent's earlier `pnpm plan:check`, workflow static validation, serialization regression and `trail --version` succeeded; they are bookkeeping/tooling checks, not launcher proof.

After recording the blocker, parent ran `pnpm plan:check && pnpm plan:status`: exit 0, valid plan, P01 blocked and no ready task. Listener checks found ports 8090/5173 clear; `git diff --cached --name-only` was empty. The sanitized model metadata capture also confirms all 51 scout responses used Sol rather than the requested Luna.

### Router root cause confirmed (read-only investigation)

Active package `pi-codex-router@0.1.4` is loaded via `~/.pi/agent/settings.json`. Its actual config is `~/.pi/agent/codex-router.json`, not the separate legacy `model-router.json`. Effective routing is enabled, fast=Luna, balanced=Luna, advanced=Sol. No project override or router environment override was present in the inspected context.

Installed `extensions/index.ts:32,69-80,119-128` starts with `manualSelection=false`, protects only subsequent selection events, and sets model/thinking in `before_agent_start`. It has no explicit-subagent-pin guard. `src/classify.ts:30-34` makes any occurrence of `lint` (among other lightweight keywords) force the simple category; `src/router.ts:17` forces low thinking for that category. The recorded implementation prompt contains `pnpm lint`, so it selects balanced/Luna/low despite architecture/security requirements. Removing that keyword in the local diagnostic changes classification to architecture. The scout prompt selects advanced/Sol/xhigh.

Command `node .artifacts/p01-t3/router-diagnostic.mjs > .artifacts/p01-t3/router-diagnostic.json` exited 0. It loaded the installed adapter/config/router, replayed both recorded initial prompts against an in-memory Pi API, and asserted both final model/thinking outcomes match the native logs. For both prompts, disabled-router and manual-selection-event controls preserved the requested model/thinking. Historical repository scans were not retained; neutral repository inputs still reproduce both outcomes. No Pi child/provider request was launched and no installed router, configuration or source-checkout files were edited. The relevant installed files match source checkout `~/dev/pi-codex-router` at `6ad94a2`; its existing modified `PLAN.md` was left untouched.

Recommended repair: preserve explicit subagent model/thinking choices, and prevent incidental verification commands such as `pnpm lint` from defining the whole task as simple. A scoped routing disable is a possible workaround, not yet applied. Native identity verification and all application safety/review gates remain required after any approved change.

The six product decisions and implementation outline were explicitly approved by the parent after reference inspection; the preserved decision packet is `.artifacts/p01-t3/approved-decisions.md`. Decisions must still be committed into the intended architecture/parity documentation and checked when implementation resumes. At that historical checkpoint P01/T3 were blocked. The approved continuation below resolves the routing/partial-code blocker; C4 remains pending and P02 may not begin.


## P01.T3 controlled writer recovery (2026-09-11)

### Authority, identity and boundaries

Continuation **bf980388-5e5a-4db2-91ee-2191cc62bc08**, sole writer **4f1ffc42-6f21-4e55-84a0-aebe5dbbec0e**, main baseline still **a8364e0063fed863b1a4871964b7d3f8a0c61f82**. Supervisor confirmed prerequisite **proc_90ab** passed at 21:41:01Z (plan/regression 7/type/backend 8), then approved the narrow repair checkpoint before application edits. Native Astra/xhigh identity barrier also passed; historical routing blockers are resolved, not hidden or worked around with a new tool/protocol. Parent documentation and accepted T2 implementation/SDK patch were preserved. Reference HEAD remains **442890dda579c6cb108d2f4851816e4388207627**, read-only; no source DB, live app, production or global-tool changes.

**Writer scope implemented and locally verified; T3 remains in_progress, C4 pending.** No staging, commit, push, deploy, release or nested agents. Parent owns fresh reviews, actual native/remote-CI acceptance and publication. This evidence describes an uncommitted content identity, not a claim that baseline HEAD contains the repair.

Final public content manifest `.artifacts/p01-t3/writer-content.sha256`: **40711ca00b36692fdee6a15e70bbef63e1213efc9d5c0c4c8f3025b02bb902cf**, 72 files, including application/tests/config/pins/decision docs. Excludes only post-run STATUS/HANDOFF/this evidence; those receive separate final plan checks. Node **26.7.0**, pnpm **11.22.0**, macOS arm64, unchanged Rust **1.91.1**. Vite **8.3.0**, SDK **0.14.1** + existing patch, WASM **0.6.0**, JCO **1.32.1** unchanged.

### Acceptance-first diagnostics and root-cause repairs

| Run / command | Exit / diagnostics |
|---|---|
| proc_2323: `pnpm test:bootstrap` against rejected partial launcher | **1**, 15 tests / **15 failures**, no skips; `.artifacts/p01-t3/writer-red.log`. Real readiness reporting, unmarked/external adoption, ports/version/concurrency/signals and missing release/decision pieces fail, replacing the previous green pair of insufficient checks |
| proc_cd29: first repair `pnpm test:bootstrap` | **1**, 6 passed / 9 failed; `writer-green-1.log`. Containment/listener preservation worked. Actual Kit `/display` returns `__sveltekit_dev`, not generic Vite `/@vite/client`; fix uses actual SPA identity. No readiness timeout/retry increase; launcher phase budget reduced to 10s within existing 20s test bound |
| proc_e660: focused `node --test --test-name-pattern='^real stack' scripts/bootstrap.test.mjs` | **1**, 1 failed; `writer-private-files-red.log`. New private-log/depot HTTP assertions passed (403); normal-stop exit **143** exposed Vite's competing standalone SIGTERM → process.exit handler |
| Installed Vite source + Context7 Vite 8 JavaScript API inspection | `node_modules/vite/dist/node/chunks/node.js` standalone closeServerAndExit calls process.exit. Use documented middleware mode with the launcher's own HTTP/HMR server, not monkey-patching handlers or nested pnpm/Vite children. Then await frontend and owned group cleanup |
| proc_38e4: `pnpm test:bootstrap` | **0**, 15/15, zero skips/retries, 37.6s; `writer-green-2.log`. Real restart/signals/failures/escalation all pass |
| First `node scripts/setup-trailbase.mjs` (proc_53f0) | **1**, verified bytes but refused unexpected ZIP members before execution. Fresh inspection showed exact `trail`, `CHANGELOG.md`, `LICENSE` in both official archives; retain strict member-list validation, extract only binary bytes to a fixed regular file |
| Second setup (proc_edf0) | **0**, actual downloaded Mac executable asserted; `writer-release-install-2.log` |
| Initial `pnpm lint` during self-review | **1**, no-unsafe-finally in test teardown. Restructured callback, no rule disabled; next and both full-gate runs pass |

No retries, blanket sleeps, raised acceptance timeouts or skipped tools. Added a **60s hard Node test-runner ceiling** to the previously unlimited command; the existing smaller state-based readiness/refusal/shutdown bounds remain unchanged. The first full suite passed before that one-line ceiling; the complete suite was repeated on final content afterward.

### What the executable checks prove

`pnpm test:bootstrap` has **15** tests: real owned TrailBase health + fresh API nonce + exact constant schema row, real Vite `/display` HTML, mode-0600 log and frontend denial of log/depot URLs; real SQLite file identity and synthetic data retained across stopped-depot restart, previous API nonce rejected; refusal of unmarked/wrong-owner/external targets and root/depot/marker/tree symlinks; numeric-port/loopback validation before writes; occupied backend/frontend listeners survive and foreign files remain intact; exact version/source/SQLite/missing-binary preflight before depot creation; simultaneous same-depot launches yield one live owner; negative schema/instance endpoints prove both actual health and nonce-schema requests reached them; spawn errors, early clean/nonzero backend exits, frontend initialization failure, post-ready backend exit; SIGINT/SIGTERM/SIGHUP; stubborn child+descendant escalation limited to the actually created group, gone before successful cleanup. Synthetic secrets are present only in private fault-child logs, absent from launcher output.

Test depots are unique `.local/dev/bootstrap-*`; test-only SQLite insertion occurs while stopped and never touches auth/gameplay tables. Development never deletes persistent data. Exclusive sibling lock covers depot creation through cleanup and is not automatically stolen. Raw backend logs stay mode-0600 under ignored `.artifacts/p01-t3/dev/`. Seven known empty locks from the failed Vite-exit run plus the marked writer diagnostic depot were removed after confirming no corresponding owned process; inventory `.artifacts/p01-t3/writer-cleanup.json`. This is not a human-depot reset.

Only source `backend/config/development.textproto` plus one constant readiness migration was added. No copied T2 WASM/audit/fault endpoint, game tables, auth UI or speculative application framework. All six **P01-D1–D6** decisions are now explicit in ARCHITECTURE/PARITY with pinned source line citations, intentional-behavior/defect distinctions and F-ID/phase mappings. Structural checks prove those documents/pins exist, not future gameplay.

### Release provenance and CI

Independent `gh api repos/trailbaseio/trailbase/releases/tags/v0.33.14` and `.../git/ref/tags/v0.33.14` exited **0**; sanitized metadata is `writer-release-{metadata,ref}.json`. Tag is the direct full commit **3f965de7ea516c43a54ca70a495e97f0c6d991ab**. Fresh downloads in proc_1ddb independently match official metadata:

| Official archive | Bytes | SHA256 |
|---|---:|---|
| `trailbase_v0.33.14_aarch64_apple_darwin.zip` | 25,282,295 | `a28454d67751863a2cce7b1477c603dc2321284d8804d9611aaa612523b5964f` |
| `trailbase_v0.33.14_x86_64_linux.zip` | 29,555,962 | `ef2f334704835dc67a95bb7f24961a0aa6ea8912d76d4ed2a09d488ddb7da453` |

Single manifest `scripts/trailbase-releases.json` supplies explicit local setup and Linux CI. Setup refuses unsupported platform/target/destination/cache substitution, verifies size/digest before unzip/execution, checks the fixed archive members and writes only a regular private executable. Exact output assertion: `trail v0.33.14-0-g3f965de7 (2026-09-10)` plus `sqlite: 3.53.2`; the official tag resolves the printed short source to the full pin. Downloaded Mac and installed `trail` both hash to **0bfe77e850e1b3a30161555c4fb34f4eecf7c0f81939fd02aa43aa29bf22eaf9**. Verified local install intentionally retained at `.local/tools/trailbase-v0.33.14-darwin-arm64`; PATH not changed automatically. Both downloaded archives remain ignored/private.

Linux archive **not executed on Mac**. CI asserts actual Linux/x86_64, uses the same explicit setup, Node 24/pinned pnpm/frozen dependencies and implemented bootstrap/backend/quality/scaffold/plan gates. No raw backend log upload. Local browser shell uses installed Chrome; pinned-browser CI is P04, native macOS/Android cannot be claimed from this Linux job. Remote CI has not run/been accepted by this writer.

### Final full regression on final content

Managed **proc_da3b**, **2026-09-11T22:11:58.554Z–22:13:03.188Z**, exit **0**; exact per-command start/end/exit in `.artifacts/p01-t3/writer-gates.json`, logs `writer-gate-0.log` through `writer-gate-10.log`. First full pass proc_b47c is separately preserved in `writer-first-full/`; final pass below includes the finite runner ceiling.

| Command | Exit | Result |
|---|---:|---|
| `pnpm test:bootstrap` | 0 | **15 passed**, 0 failed/cancelled/skipped; 38.35s |
| `pnpm check` | 0 | 0 Svelte errors/warnings |
| `pnpm lint` | 0 | No diagnostics |
| `pnpm test:unit` | 0 | **2 passed** |
| `pnpm build` | 0 | Shared adapter-static build |
| `pnpm test:backend -- capabilities` | 0 | **8 passed**, real pinned backend, port **64841**, 0 skipped |
| `pnpm test:scaffold` | 0 | **3 passed**, 0 skipped |
| `pnpm test:shell` | 0 | **3 passed**, installed Chrome **153.0.8010.37**, fresh contexts, zero retries/skips |
| `pnpm plan:check` | 0 | 13 phases / 39 tasks / 52 criteria valid |
| `pnpm plan:status` | 0 | P01/T3 in_progress; C4 pending; P02 unstarted |
| `pnpm test:plan` | 0 | **7 passed**, 0 skipped |

Actually tested rebuilt T2 component SHA256 **3d35a426e93c8c20bd4a6a73ce1bbe6d206c5004746a4f4fb54f5db08bee1097** (build bytes legitimately vary; no bit-for-bit reproducibility claim). No source change to accepted T2 or its patch. Browser shell still proves navigation/preferences, not auth/SSE/gameplay.

### Self-review and residual gates

Reviewed full launcher/setup/path helper, all tests, source migrations/config, workflow, decision citations and actual diff. No known current-scope blocker after fixes. `git diff --check` passed; staged list empty. Final process/listener/depot and post-document plan checks are recorded in the handoff/report. No owned PIDs/ports or synthetic test depots should remain.

Limits: cooperative local ownership is not a sandbox against a hostile same-UID process concurrently replacing files; uncatchable SIGKILL/power loss may leave a stale lock requiring inspection. No automatic stale-lock stealing/reset. **Required fresh spec/security reviews, parent actual Tauri shared `/display` window + cleanup, and remote Linux CI execution remain pending**. C4/P01/T3 must not be marked complete from this writer report. Google/SMTP, corpus rights/import, gameplay, Android/signing/updater/hardware remain their original later gates, not implemented or silently skipped.

Final post-document check at **2026-09-11T22:15:50Z**: `pnpm plan:check`, `pnpm plan:status`, `pnpm test:plan` (7), `git diff --check`, `git diff --cached --quiet` all exit **0** (`writer-final-docs.log`). Content hashes rechecked unchanged; **51** new private backend logs all mode-0600; owned process list empty, `.local/dev/` and `.local/test-runs/` empty; ports **64841, 4173, 8090, 5173, 64131, 64132** bindable. Safety record `.artifacts/p01-t3/writer-final-safety.json`. No staged files or owned PIDs/ports remain; verified local release install and private artifacts intentionally persist.

## P01.T3 correction pass one — bounded version probe (2026-09-12)

The fresh quality review `fixes_needed` finding is preserved at `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/subagent-artifacts/outputs/4ce6e9ec-11dc-468b-8d2f-9edc94df4fc0/p01-accepted-thinking/quality.json`: a PATH-resolved `trail --version` process that ignores SIGTERM could leave `execFile` pending forever, while launcher cancellation did not reach it. Before implementation, the new stubborn-probe regression was run against that behavior; the command timed out after 120 seconds with zero passing tests and left only its test-owned probe groups, which were then terminated by exact owned process-group IDs. This is the observed red, not an inferred failure.

The correction changes only `scripts/setup-trailbase.mjs`, `scripts/dev.mjs`, and `scripts/bootstrap.test.mjs`. The shared version helper now owns a real spawned child, keeps the existing **5000ms** timeout, terminates on timeout/cancellation, escalates to SIGKILL after a **100ms** grace, and resolves/rejects only after the child's `close` event. `runDev` passes its startup AbortSignal into that helper. New tests use a unique PATH-only executable that records its own PID in a mode-0600 test-owned barrier, ignores SIGTERM, and prove timeout plus SIGINT/SIGTERM/SIGHUP cancellation: the launcher exits within the existing 8s bound, no `Ready:` is emitted, no depot is created, and the recorded probe is reaped.

Content identity (SHA-256): `scripts/setup-trailbase.mjs` 32cb53e85a6ef81bfed2c4029da5f3c2aa5da45b609bf53b5ff336d4b65c8456; `scripts/dev.mjs` 0320bf11072eadc0bb0200afe7682c195a1959b09ca3f41ef9ce91697e22c0f2; `scripts/bootstrap.test.mjs` 55db4db92c4f4975b4adc2cd4d20eaa1b1da99bcdc2e908244996e58a79fc57b. The pre-existing unrelated worktree changes remain untouched.

Final correction checks (uncommitted main content; no staging/publication):

| Command | Exit / result |
|---|---|
| focused stubborn/cancellation tests | 0; 4 passed, 0 failed/skipped |
| `pnpm test:bootstrap` (`proc_9e74`) | 0; **19 passed**, 0 failed/cancelled/skipped; 44.36s |
| `pnpm check` | 0; 0 Svelte errors/warnings |
| `pnpm lint` | 0; no diagnostics |
| `pnpm test:unit` | 0; 2 tests passed |
| `pnpm build` | 0; adapter-static build |
| `pnpm test:backend -- capabilities` | 0; 8 real pinned-backend tests passed, port 52651 |
| `pnpm test:scaffold` | 0; 3 passed |
| `pnpm test:shell` | 0; 3 passed, Chrome 153.0.8010.37 |
| `git diff --check` | 0 |

Private logs are `.artifacts/p01-t3/version-correction-{red,quality,integration}.log` and managed process logs; no credentials are included in this report. At this successful intermediate checkpoint, final process/listener/depot inspection found no owned processes, test depots or locks; no application process was started. Remaining gates were the fresh Astra/xhigh quality/security review, parent-owned native macOS shared-window/cleanup, remote Linux CI and parent acceptance; P01.T3 was still in_progress, C4 pending, and P02 unstarted. The later SIGINT result below supersedes that intermediate status.

## Correction checkpoint — unresolved SIGINT teardown (blocked)

The later final-gate run `proc_ff8b` must be recorded separately: `pnpm test:bootstrap` exited **1** with **18 passed / 1 failed**, the existing real-stack `SIGINT closes real frontend and backend before exit, preserving depot` test. A separate focused batch also reproduced this once (`RUN=4`; terminal assertion included the Ready line and `Owned stack cleanup failed`; no private transcript was written for that loop). These failures are not accepted as flakes.

A bounded diagnostic batch then added temporary instrumentation in `scripts/dev.mjs` only: each cleanup promise was labelled (`frontend`, `http`, `backend`) and the original exception was written to `${logPath}.cleanup-error` with mode **0600**, while terminal output stayed sanitized. Exactly **10/10** focused SIGINT runs exited 0, so no operation or exception was captured. The instrumentation remains deliberately in the working tree as **unaccepted** state pending fresh assessment; it is not a fix, and no all-settled or other speculative correction was applied. Current source hashes after instrumentation are recorded in the bound checkpoint report.

Accordingly P01.T3 is now **blocked** on an unidentified intermittent SIGINT teardown/cleanup failure; C4 remains pending and P02 remains unstarted. Next action is a fresh **Astra/xhigh concurrency/cleanup assessment**, then only an evidence-backed minimal correction. Parent/native macOS, remote Linux CI and acceptance remain pending. The earlier `proc_9e74` 19/19 green and later `proc_ff8b` 18/19 failure remain distinct facts.

## Documentation alignment — owner clarification (2026-09-11)

This section records the documentation-only continuation. Actual identity was verified **before edits** with `printf 'PI_PROVIDER=%s\\nPI_MODEL=%s\\nPI_REASONING_LEVEL=%s\\n' ...`: **`openai-codex/gpt-5.6-luna` / `high`**. Only `AGENTS.md`, `docs/STATUS.json`, `docs/HANDOFF.md` and this file were edited. No application source, application tests, server, settings, router/configuration, alias, provider, execution protocol, staging or publication was changed.

### Orchestration history (not application-test evidence)

- Repair writer **`4f1ffc42-6f21-4e55-84a0-aebe5dbbec0e`** completed. Its full regression **`proc_da3b`** passed at **2026-09-11T22:13:03Z**: bootstrap 15, unit 2, backend 8, scaffold 3, Chrome shell 3 and plan 7; zero skips/retries. These are writer results, not new parent native/remote-CI proof.
- Fresh specification reviewer **`43aa9500-2a33-426b-91a4-a3e0011e27cb`** returned ready/no findings. Actual report: `/Users/markb/.pi/agent/sessions/--Users-markb-dev-trail-party--/subagent-artifacts/outputs/bf980388-5e5a-4db2-91ee-2191cc62bc08/p01-t3-resume/spec-0.json`.
- Workflow **`bf980388-5e5a-4db2-91ee-2191cc62bc08`** then failed `JSON.parse(result.output)` at position **1964** because the harness appends an **Output saved** footer. No quality review launched. This is not a JavaScript source-syntax defect or application failure. Snapshot `.artifacts/p01-t3/post-writer-workflow-failed.patch` and `post-writer-untracked.tgz`, with receipt `post-writer-workflow-failed.receipt.json`, preserve that state; the parent reads the bound report rather than parsing wrapped display text.
- Luna/high task **`e99a6ded-4690-4a25-8016-5f9a5616706f`** wrote the dispatch policy. The parent restored two accidentally removed safeguards and reran structural/plan checks: **7 passing tests**; no unrelated tracked/untracked content changed.
- Workflow **`e43203a2-5326-4419-8668-da92b461131e`** stopped at identity-blocked before security review. Probe **`42e2794c-3958-4278-9b9a-1f25df81b510`** reported actual `openai-codex/gpt-6-astra`/`xhigh`. Native metadata proves **max** selected at **2026-09-11T22:41:31.062Z**, changed to **xhigh** at **22:41:31.209Z**, before the first response; diagnostic `.artifacts/p01-t3/astra-max-identity-diagnostic.json`. Under the owner's clarification, xhigh is accepted; this is not a current blocker and must not be described as max or retained as a resolved blocker.
- Latest startup checks confirmed `pwd`/main/HEAD/reference baseline, exact `trail --version`, and an empty managed process list. No application process was started for this documentation-only alignment.

### Alignment red/green and current state

Before editing, `node .artifacts/p01-t3/alignment-structural-check.mjs` exited **1** and captured `.artifacts/p01-t3/alignment-red.log`; the missing-policy structural red was the malformed pre-alignment `docs/STATUS.json` plus absent clarified policy/current-state assertions. After editing, the same structural check passed. The required document checks all passed with exit **0**: `pnpm plan:check`, `pnpm plan:status`, `pnpm test:plan` (**7 passed**) and `git diff --check`. Application suites were intentionally not rerun for this document-only change.

Current state is **P01/T3 in_progress**, **P01.C4 pending**, and **P02 unstarted/pending**. The next step is a fresh **Astra/xhigh** quality/security review; **Astra/xhigh or Astra/max is acceptable, max is not mandatory**, and the working explicit example is `openai-codex/gpt-6-astra:xhigh`. Then parent-owned real native macOS shared-window/cleanup, full parent acceptance and remote Linux CI remain required; final phase review remains required. No app acceptance or P01 completion claim is made. Residual gates include native macOS, remote Linux CI, quality/security review and parent acceptance; Google/SMTP, corpus rights/import, gameplay, Android/signing/updater/hardware remain later gates.

## Independent cleanup repair — Luna/high writer (2026-09-12)

Identity was verified before application edits from active runtime metadata: `PI_PROVIDER=openai-codex`, `PI_MODEL=gpt-5.6-luna`; thinking level was `high`. The sole writer changed only the launcher, bootstrap regression/fixture and governance evidence; no settings, pins, setup script, publication, staging or commit was performed. Existing unrelated work remains untouched.

Acceptance-first focused regression: the new `cleanup ownership` cases were red against the defective launcher: **2 tests, 0 passed / 2 failed**, with synchronous frontend failure skipping `backend-term-entered` and asynchronous rejection returning before held-backend release. After the minimal cleanup-driver repair, the focused command passed **2/2, zero skips**. It covers synchronous invocation of a frozen `Error`, asynchronous rejection with a primitive reason, exact detached backend PID/group IPC, HTTP listening barrier, held backend/lock, release ordering, owned log-FD closure, private mode-0600 diagnostics, nonzero result and sanitized terminal output. Test-owned orphan locks/processes from the defective red run were removed by exact recorded paths/groups.

Repair evidence: cleanup invokes frontend/http/backend through separately protected invocation/settlement boundaries, always awaits independent operations, closes log independently, and removes the lock only after backend termination succeeds. Diagnostics are private `${logPath}.cleanup.json` records with bounded original Error fields/stack or primitive reason and operation labels `frontend`, `http`, `backend`, `log.close`, `lock.remove`; no foreign exception mutation or terminal synthetic details remain. Legitimate `ERR_SERVER_NOT_RUNNING` partial HTTP startup is recorded as `not-listening`; other HTTP callback failures reject independently. Diagnostic serialization/write failure cannot turn cleanup success or failure into the opposite result.

Current affected gates: `pnpm check` **0** (0 diagnostics), `pnpm lint` **0** after the required comment/unused-catch cleanup, `pnpm test:unit` **0** (2/2), `pnpm build` **0**, `pnpm test:backend -- capabilities` **0**, `pnpm test:scaffold` **0** (3/3), `pnpm test:shell` **0** (3/3), `pnpm plan:check` **0**, `pnpm plan:status` **0**, `pnpm test:plan` **0** (7/7), final focused cleanup command **0** (2/2), `git diff --check` **0**, and `git diff --cached --quiet` **0**. One authorized full bootstrap run was **1**, **20/21 passed**, with the existing real SIGTERM normal-stop case failing on `backend` `kill EPERM` in `groupAlive`; private artifact `.artifacts/p01-t3/dev/run-ut4DfJ/trail.log.cleanup.json` preserves the original operation/error. The historical SIGINT cause remains unresolved; the SIGINT case passed in that run, which is not retrospective root-cause evidence and does not accept C4.

P01/T3 returns to **blocked** after this writer because the historical blocker remains and no next historical-cause writer is approved; C4 stays pending and P02 unstarted. Required fresh Luna/high task-level review remains parent-owned. No native macOS, remote Linux CI or phase acceptance is claimed.

## Signal-zero EPERM continuation — first correction cycle (2026-09-12)

The retained full-run artifact `.artifacts/p01-t3/dev/run-ut4DfJ/trail.log.cleanup.json` attributes `Error: kill EPERM` / `code: EPERM` to the backend `groupAlive` signal-zero probe, not the earlier SIGINT exception. Before this cycle, hashes were recorded in `.artifacts/p01-t3/cleanup-probe-pre-repair-hashes.sha256`: `dev.mjs` 7d69371495a1e70e560a748492a005a1ae84598252f140e67ebf5f9ad39c0aec, `bootstrap.test.mjs` 681cc1ca853a2ff4ce6dc34182ea94c4c647995f9d436586df7d63809d435e2e, fixture 4e58479f70ec8384e1d17aea74d121951f5f9e25e4d6eee4e72c04a6bc0c1341. The failed run's backend PID/group had no remaining process (`ps` showed no PID 24157) before its exact retained lock was manually removed; no unknown process was killed.

Node's documented `process.kill` behavior and POSIX `kill(2)/kill(3p)` semantics were checked: signal 0 performs existence/permission checking, `ESRCH` confirms no matching process, while `EPERM` means a matching target may still exist and must not be treated as absent. EPERM cannot be reliably induced on demand under current permissions; the regression therefore uses only an owned fixture and a narrow fixture-local wrapper, delegates all other calls, and restores the native function. It injects one transient signal-zero EPERM then requires native disappearance, persistent signal-zero denial that exhausts existing 5s/1s bounds and retains the lock, and real SIGTERM send denial that remains nonzero/retains the lock. Fallback cleanup uses unmodified real signaling on the exact recorded group.

Red against the pre-correction helper: the focused `cleanup ownership` command was **5 tests, 2 passed / 3 failed**, with transient EPERM finishing before native disappearance and persistent/send-denial evidence exposing the unresolved helper branch. After the minimal shared `groupAlive` correction (`EPERM => present`, never success/absence), focused green was **5/5, zero skips**. One authorized post-correction `pnpm test:bootstrap` passed **24/24, zero skips**; all real SIGINT/SIGTERM/SIGHUP lifecycle cases passed in that run. This pass does not identify or accept the historical SIGINT cause. The persistent-denial test confirms lock retention and the signal-denial test confirms nonzero/retained ownership; all test-owned locks/processes were cleaned exactly afterward.

Post-correction hashes: `dev.mjs` 47acb790c23d561ab5541c904aca67511724a0528d6087a910138069b35e3092, `bootstrap.test.mjs` 4f3d7c044cc8dcb067d36bee84076e409a556abbda4c49af3fb771a86d3d1368, fixture 10e5fe1d07875e596d86af80dd2e0bf8ce3eb52a2ba293c36d02a09af30fc3c2. Affected post-correction gates passed: check, lint, unit (2), build, backend capabilities (8), scaffold (3), Chrome shell (3), plan check/status, plan tests (7), diff check and empty staging. Required fresh Luna/high review still follows; no historical SIGINT, native macOS, remote Linux or C4 acceptance is claimed.

## Parent-controlled native macOS verification (2026-09-12)

Actual runtime identity was verified before the native run: `PI_PROVIDER=openai-codex`, `PI_MODEL=gpt-5.6-luna`, `PI_REASONING_LEVEL=high`. The current worktree was on `main` at `a8364e0063fed863b1a4871964b7d3f8a0c61f82`; `trail --version` returned v0.33.14-0-g3f965de7 with SQLite 3.53.2. No pre-existing Trail Party-owned process or listener was found on ports 5173/8090.

Managed process `proc_ec4c` ran only `pnpm tauri dev --no-watch`. It emitted `Ready:` for backend `127.0.0.1:8090`, frontend `127.0.0.1:5173`, backend PID 32418 and private log `.artifacts/p01-t3/dev/run-7NSeUl/trail.log`; the Tauri process tree included application PID 32430 in owned process group 32225. CoreGraphics/System Events identified one owned window titled `Trail Party Display` at 1280x720, and its private window-only screenshot visibly rendered the shared `/display` shell heading/content/theme controls. Screenshot: `.artifacts/p01-t3/native/native-display.png`, mode 0600, SHA256 `c5715251615112cfe7a6cf3d2f2a49a80edf241d960c3b4dd8be722e9b467b52`.

The managed stop action stopped the owned process group; it reported `killed` and pnpm emitted `[ELIFECYCLE] Command failed`, so a clean native launcher exit was **not** established. Post-stop inspection found no owned Trail/Tauri/Vite/node processes, ports 5173 and 8090 were clear, and `.local/dev/depot.lock` plus sibling locks were absent; the persistent `.local/dev/depot` was retained. Private process logs are `.artifacts/p01-t3/native/proc_ec4c-{stdout,stderr}.log`, mode 0600. Cargo supporting gates ran in managed `proc_44e5`: `cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo build --locked --manifest-path src-tauri/Cargo.toml`, exit 0.

This proves actual native `/display` rendering and owned-resource cleanup, but not clean same-invocation shutdown. The historical SIGINT blocker remains unresolved; P01/T3 remains blocked, P01.C4 remains pending, and P02 remains unstarted. No code/settings/dependency changes, staging, commit, push, remote Linux or packaged-app acceptance occurred.


## P01.T3 native evidence alignment (2026-09-12)

The fresh Luna task-level review is consumed: actual provider/model/reasoning was `openai-codex/gpt-5.6-luna` / `high`; verdict `ready`, no findings, `phase_accepted:false`. The managed native run `proc_ec4c` rendered the shared `/display`, and its screenshot is evidence only for that managed render run. Managed stop was `killed` with ELIFECYCLE, so no clean same-invocation exit is claimed.

The separate direct diagnostic `proc_fd95` sent SIGINT to exact owned Tauri group PGID 37631 and exited 0; TrailBase logged graceful shutdown, ports/locks cleared, and persistent depot retained. This distinguishes that run's shutdown behavior only. It does not prove packaged native, remote Linux, Android TV, C4/P01 completion, or historical SIGINT-cause resolution. The historical blocker remains preserved and P01/P01.T3 remain blocked; C4 remains pending and P02 remains unstarted.

The requested-Luna/observed-Astra resume mismatch is recorded as a technical orchestration limitation only; no Astra result is accepted and explicit owner approval remains required before any Astra phase review. Source reports: `.artifacts/p01-t3/native-acceptance-report.md`, `.artifacts/p01-t3/native-stop-diagnostic-report.md`, and `.artifacts/p01-t3/cleanup-repair-contract.md`.
